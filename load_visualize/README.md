## Task testing for load function

- install requirements in a venv
- run main.py to see the load graph with : streamlit run main.py

it should work =)

i've tried multiple ways to show the load but 'im not sure which one represent it the best, so i just put them all 
Everything has to be done manually but we can later implement some test that will show only the graphs depending on the "story/scenarios" we want to create



